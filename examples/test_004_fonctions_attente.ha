# TEST 004: Fonctions (EN ATTENTE D-IMPLÉMENTATION)
# Ce test montre la syntaxe des fonctions mais ne peut pas encore s-exécuter
# Les fonctions ne sont pas encore implémentées dans l-interpréteur

# Voici la syntaxe pour quand les fonctions seront implémentées :

# fara
#     rubuta "=== Test Fonctions ==="
#     
#     # Fonction simple sans paramètres
#     aiki saluer() {
#         rubuta "Sannu da zuwa ga Dabara!"
#         rubuta "Wannan function ba ta da parameters"
#     }
#     
#     # Fonction avec un paramètre
#     aiki saluer_mutum(sunan) {
#         rubuta "Sannu "
#         rubuta sunan
#         rubuta "!"
#     }
#     
#     # Appels des fonctions
#     saluer()
#     saluer_mutum("Ahmad")
# ƙare

# EN ATTENTE : Test simple qui fonctionne actuellement
fara
    rubuta "=== Fonctions pas encore implémentées ==="
    rubuta "Les fonctions seront disponibles dans une future version"
    rubuta "Syntaxe : aiki nom_fonction(paramètres) { corps }"
    rubuta "Appel : nom_fonction(arguments)"
ƙare